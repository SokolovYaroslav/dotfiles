# 1Password secret references (no secret material here; safe to keep in plaintext).
# Resolved at runtime by: op run --account <account> --env-file this-file -- <cmd>
# Format: op://<vault>/<item>/<field>. Get the exact ref from the 1Password app:
#   right-click the field -> "Copy Secret Reference". Adjust below if vault/item/field differ.
ANTHROPIC_API_KEY=op://{{ .opVaultAnthropic }}/Anthropic API key dev/credential
