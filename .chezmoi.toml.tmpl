{{- /* Wizard prompts. Answers are stored in chezmoi's persistent state and asked only once. */ -}}
{{- $opAccount := promptStringOnce . "opAccount" "1Password account shorthand (e.g. my.1password.com)" -}}
{{- $opVaultYT := promptStringOnce . "opVaultYT" "1Password vault that holds the YouTrack token (item: 'YT Token')" -}}
{{- $opVaultAnthropic := promptStringOnce . "opVaultAnthropic" "1Password vault that holds the Anthropic API key (item: 'Anthropic API key dev')" -}}
{{- $useOpSsh := promptBoolOnce . "useOnePasswordSshAgent" "Use the 1Password SSH agent (also used for git commit signing)?" -}}
{{- $enableJbProxy := promptBoolOnce . "enableJbProxy" "Set up the JetBrains AI proxy via jbcentral?" -}}
{{- $jbProxyBaseUrl := "" -}}
{{- if $enableJbProxy -}}
{{-   $jbProxyBaseUrl = promptStringOnce . "jbProxyBaseUrl" "ANTHROPIC_BASE_URL for the JetBrains proxy (from your jbcentral setup)" -}}
{{- end -}}
{{- $installGh := promptBoolOnce . "installGh" "Install the GitHub CLI (gh) and run 'gh auth login'?" -}}
{{- $enableMcp := promptBoolOnce . "enableMcp" "Register the YouTrack MCP server for Claude Code?" -}}

[data]
    opAccount = {{ $opAccount | quote }}
    opVaultYT = {{ $opVaultYT | quote }}
    opVaultAnthropic = {{ $opVaultAnthropic | quote }}
    useOnePasswordSshAgent = {{ $useOpSsh }}
    enableJbProxy = {{ $enableJbProxy }}
    jbProxyBaseUrl = {{ $jbProxyBaseUrl | quote }}
    installGh = {{ $installGh }}
    enableMcp = {{ $enableMcp }}
